# Migration Tools Dependencies
# Python packages needed for database migrations

# HTTP requests for Elasticsearch migrations
requests>=2.28.0

# PostgreSQL adapter (already likely installed)
psycopg2-binary>=2.9.0

# Alembic for SQL migrations (already likely installed)
alembic>=1.8.0

# Rich for CLI formatting (already likely installed)
rich>=12.0.0

# Click for CLI (already likely installed)
click>=8.0.0 